import React from 'react';

import PolicySection from '../Policies/Policies';

const Main = () => (
    <div className="if block color secondary lighterBeige">
        <div className="if container">
            {/* Space for other sections, header, etc */}
            <PolicySection />
        </div>
    </div>
);

export default Main;
